# Privacy & network audit (task 063)

Generated by `bash scripts/privacy-audit.sh` on 2026-09-07 13:35 UTC.

Rasoi promises that nothing about the household leaves the phone, and that the only
network-capable code is Apple's MapKit store search (SPEC R1). This is that promise,
checked mechanically.

## Networking APIs in the sources

- ✅ No networking, analytics or web-view APIs anywhere in the sources

## The one allowed network path

`Rasoi/Services/StoreFinder.swift` imports:

```
import CoreLocation
import Foundation
import MapKit
```
- ✅ The store finder imports only Foundation, MapKit and CoreLocation
- ✅ The store finder uses MKLocalSearch/CLGeocoder only — no raw networking

## Dependencies

- ✅ No package manifest and no bundled framework — zero third-party code
- ✅ project.yml declares no Swift packages

## Entitlements and background modes

- ✅ No entitlements file: no iCloud, no push, no app groups (SPEC R7)
- ✅ No background modes and no App Transport Security exceptions

## The built binary

Linked libraries:

```
@rpath/Rasoi.debug.dylib
/usr/lib/libSystem.B.dylib
```
- ✅ The binary links only Apple system libraries and the Swift runtime

## Reachability of the store finder

Files that mention the finder at all:

```
Rasoi/Features/Stores/FindStoresSheet.swift
Rasoi/Services/StoreFinder.swift
```
- ✅ Reached only from More › Stores › Find nearby, and only when the household taps Search

See docs/PRIVACY-MAPKIT.md for exactly what that search sends.
