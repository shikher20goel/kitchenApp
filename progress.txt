Session start. No tasks complete yet. Kit generated 2026-09-07 by the orchestrator (SPEC, plan, prd, seed data).
# Append one line per task below, e.g.:
# TASK 000 done: repo initialised on autonomous-build, kit committed.
# TASK 028 implemented, awaiting human review (seed recipes).
TASK 000 done: repo initialised on autonomous-build, kit committed and pushed.
TASK 001 done: RasoiApp.swift + Assets.xcassets added; xcodegen generates 3 targets.
TASK 002 done: ContentView 5 tabs + Theme design tokens; generic iOS Simulator build green.
TASK 003 done: verify.sh green on iPhone 17 sim, 3 test cases (SmokeTests x2 + LaunchUITests).
TASK 004 done: configure-signing.sh free/full/none touch only the DEVELOPMENT_TEAM line; verify.sh still green.
TASK 005 done: Persistence.swift (schema registry, shared + inMemory), TestContainer helper, PersistenceTests green.
TASK 006 done: AgeBandTests + HouseholdMemberTests written first (red until 007), plus D date fixtures.
LESSON (task 007): SwiftData traps (EXC_BREAKPOINT inside SwiftData, "Restarting after unexpected
  exit") on the first insert/fetch when the ModelContainer is not retained — a ModelContext does
  NOT keep its container alive. RasoiTests/Support/TestContainer.swift therefore hands tests a
  TestStack (container + context); never write `let ctx = try TestContainer.make().mainContext`.
  Second gotcha found the same hour: do NOT pass `schema:` to ModelConfiguration and also to
  ModelContainer(for:) — build the container as ModelContainer(for: schema, configurations:
  ModelConfiguration(isStoredInMemoryOnly:)).
TASK 007 done: HouseholdMember model + AgeBand engine + Calendar.rasoi; 18 unit tests green.
TASK 008 done: DietProfileTests written first (defaults, singleton idempotence, exclusions, appliances).
TASK 009 done: DietProfile singleton + Appliance vocabulary; 23 unit tests green.
TASK 010 done: StoreTests written first (6 defaults, idempotent re-seed, sortOrder, preferred flags, affinity).
NOTE (task 011): plan.md said supermarket/warehouse affinity = "everything else". Implemented as
  ALL categories instead (they really do stock everything) and made speciality store kinds win
  first in store assignment; the six defaults ship with only Walmart/Costco/Stop & Shop preferred,
  so nothing routes to an address-less speciality shop until the household opts in.
TASK 011 done: Store + StoreKind + IngredientCategory + StoreSeeder (6 idempotent defaults); 29 tests green.
TASK 012 done: IngredientTests written first (category round-trip, unique name, type-ahead incl. aliases).
TASK 013 done: Ingredient (+MeasurementUnit vocabulary, alias type-ahead, unique name); 36 tests green.
TASK 014 done: ShelfLifeTests + PantryItemTests written first (freezer x6, expiring-soon boundary, upsert per ingredient+location).
TASK 015 done: PantryItem (upsert per ingredient+location, unit conversion, consume floors at 0) + ShelfLife engine; 48 tests green.
TASK 016 done: RecipeTests written first (3 ingredients, totalMinutes, isQuick boundary, seedID uniqueness, scaling).
TASK 017 done: Recipe (+MealType, RecipeSource, inline Codable RecipeIngredient, scaling); 56 tests green.
TASK 018 done: MealPlanTests written first (weekStart Monday incl. Sunday, slot uniqueness, feedback upsert, reaction weights).
TASK 019 done: MealPlan/MealSlot/MealFeedback with Monday weekStart, per-day+meal slot upsert, per-member feedback upsert; 68 tests green.
TASK 020 done: GroceryListTests written first (unique weekStart, item ownership, check stamps checkedAt, cascade delete).
TASK 021 done: GroceryList/GroceryItem with unique weekStart, cascade items, setChecked timestamping; 74 tests green.
TASK 022 done: AppSettingsTests written first (SPEC defaults, idempotent singleton, master switch).
TASK 023 done: AppSettings singleton (reminder toggles/times, master switch, appearance); 80 tests green.
TASK 024 done: SeedLoaderTests written first (counts, vocabulary, vegetarian sweep, allergen flags, meal/appliance coverage, seedID uniqueness).
NOTE (task 025): edited seed/recipes.json — dn-pasta-hidden-veg "Red chili powder" had quantity 0
  (the only zero-quantity line in the catalog); set to 1 g. Also refined SeedLoaderTests so a
  recipe's allergen flags are checked against its REQUIRED ingredients only: seven recipes carry
  an optional yogurt/naan/pita/cheese/peanut garnish and are correctly unflagged.
TASK 025 done: SeedDTOs/SeedLoader/SeedImporter (idempotent upsert, keeps favourites + user recipes); 99 tests green.
TASK 026 done: DietFilterTests + engine fixtures written first (each rule alone, alias exclusion, appliance, min age, combined).
TASK 027 done: DietFilter + engine snapshots (RecipeSummary, IngredientFacts/Index, DietRules); 114 tests green.
TASK 028 implemented, awaiting human review (docs/SEED-RECIPES.md — 51 recipes, 185 ingredients; regenerate with scripts/gen-seed-doc.py).
TASK 029 done: FamilyViewModel (+10 tests), FamilyListView, MemberEditView, ChipField/FlowLayout design components.
LESSON (task 030): same container-lifetime trap as task 007, now via a test helper that returned
  (stack, viewModel) and was destructured with `let (_, model)` — the discarded stack deallocated
  the container and every model object trapped with "destroyed by calling ModelContext.reset".
  Test helpers must hand back ONE value that owns the container. Also: a hand-built Calendar has
  no locale, so weekdaySymbols came back abbreviated — Calendar.rasoi now sets .autoupdatingCurrent.
  And killing verify.sh mid-build corrupts build/DerivedData ("disk I/O error" on build.db):
  rm -rf build/DerivedData and re-run.
TASK 030 done: DietSettingsViewModel (+12 tests: zip validation, alias-canonical exclusions, appliance/cuisine toggles, clamps), DietSettingsView, AppCopy started.
TASK 031 done: RootView bootstrap (singletons, store seed, catalog import), 4-page OnboardingFlow, UITestSupport launch flags, OnboardingUITests green.
TASK 032 done: MoreView (grouped list, placeholders for later screens), AboutView with verbatim R1/R2 notes, DataManager.deleteEverything double-confirmed (+2 tests).
TASK 033 done: PantryViewModelTests written first (alias search, expiry from shelf life, increment, ran-out staple rule, sorts, filters).
TASK 034 done: PantryViewModel (alias search, upsert add, mark used, ran-out staple rule, expiring window, sorts); 11 tests green.
TASK 035 done: PantryView (location segments, expiring-first section, steppers, swipe mark-used/ran-out, add sheet with alias search); PantryUITests green.
TASK 036 done: RecipesViewModel (+10 tests: combining filters, alias search, ineligible hidden by default with reasons), RecipeListView, RecipeDetailView with pantry ticks.
TASK 037 done: RecipeDraft (validation + allergen/nutrition tags derived from ingredients) + RecipeEditorViewModel (+11 tests), RecipeEditView wired into list and detail.
TASK 038 done: Scaling engine (+9 tests: kitchen rounding, determinism), CookModeView (paged steps, idle timer off, servings stepper), CookModeUITests green.
TASK 039 done: KidScoreTests written first (baseline blend, exact blended deltas, 60-day half-life, household mean, resting rule + expiry, determinism).
TASK 040 done: KidScore engine (60-day half-life, baseline blend n/(n+3), household mean, 30-day resting rule); 18 tests green.
TASK 041 done: FeedbackViewModel (+8 tests: per-member upsert, unanswered = no row, cooked once, depletion hook protocol), FeedbackSheet with warm R3 copy in AppCopy.
TASK 042 done: Units (general conversion table, sum by dimension, display) +8 tests; PantryDepletion (required-only, scaled, expiry-first draw, staple floor) +11 tests; wired into the feedback sheet.
TASK 043 implemented, awaiting human review (docs/COPY-FEEDBACK.md — 16 strings; regenerate with scripts/gen-copy-doc.py feedback).
TASK 044 done: MealPlannerTests written first: one test per SPEC §5 rule (variety, weekday cap, pantry-first, use-it-up, kid score, quotas, nutrition, breakfast rotation), locking, determinism, real-catalog smoke test.
NOTE (task 045): SPEC §5 rule 4 says "≤2 new recipes per week", but on a fresh install every
  recipe is new and SPEC §11 requires a full 7-day plan. Implemented as a tiered relaxation: the
  cap holds whenever the catalog offers enough known recipes, and gives way rather than leaving a
  family without dinner. MealPlannerTests covers both directions.
TASK 045 done: MealPlanner (tiered constraints, pantry-first/use-it-up, kid-score ranking, quotas, nutrition nudges, breakfast rotation, per-slot reasons, deterministic id tie-break); 23 tests green.
TASK 046 done: PlanViewModel bridges SwiftData to the planner (snapshots, kid scores, resting, history) +13 tests; MealPlanner gained a ranked alternatives API; weights reordered to SPEC §5 rule priority.
LESSON (task 047): a `.bottomBar` toolbar item inside a NavigationStack that lives in a TabView
  sits on top of the tab bar — the UI test's tap on "Generate week" selected the Pantry tab
  instead. Primary actions on a tab screen go in the content or the navigation bar, never the
  bottom bar.
TASK 047 done: PlanView (7 day sections x 4 meals, summary card, generate/regenerate), SlotSheet (why-this, lock, servings, status, ranked swaps); PlanUITests green.
TASK 048 done: GroceryBuilderTests written first (aggregate, scale, normalise, alias merge, pantry subtraction, staple top-ups, store affinity, stable order, idempotent merge).
TASK 049 done: GroceryBuilder (aggregate+scale+normalise, pantry subtraction, staple pack sizes by dimension, speciality-first store assignment, idempotent merge that protects checked and manual lines); 19 tests green.
TASK 050 done: ShopViewModel (idempotent build/merge, check restocks pantry with shelf life, uncheck reverses, manual adds survive, store→category sections, share text) +11 tests; ShopView + ShopUITests green.
TASK 051 done: StoresViewModel (+7 tests: reorder persists, preferred toggle, affinity editing, safe removal) with StoresView + StoreEditView.
TASK 052 done: StoreFinding protocol, MapKitStoreFinder (the only network path, R1), pure StoreSearch helpers (queries, kind inference, dedup, radius, ordering) +11 tests through a fake.
TASK 053 implemented, awaiting human review (FindStoresSheet + docs/PRIVACY-MAPKIT.md; needs a real 07302 search in the simulator).
TASK 054 done: NutritionCoverageTests written first (group mapping from categories+tags, per-day coverage, weekly lightest group, MyPlate target table per age band).
TASK 055 done: NutritionCoverage (MyPlate groups from tags+categories, per-day dots, weekly lightest group, age-band target table kept out of the UI per R2); 12 tests green.
LESSONS (task 056, SwiftUI presentation — cost several hours, do not relearn):
  1. A view can own only ONE presentation of a kind. Two `.sheet`s (or two `.fullScreenCover`s)
     on the same view: the second never opens. Use one modifier with an Identifiable enum, or
     push instead.
  2. A presentation requested while another is dismissing is dropped — cook mode -> feedback had
     to become cook-mode cover, then a NavigationLink push (FeedbackView, not a sheet).
  3. `sheet`/`fullScreenCover` driven by state that nothing in `body` reads may not present at
     all. PlanView tints the open row (`slot === editingSlot`), which both looks right and gives
     the body the dependency it needs.
  4. A `.bottomBar` toolbar item inside a TabView sits on the tab bar (task 047), and a primary
     action inside a scrollable List can start off-screen: put it in the navigation bar.
  5. UI tests: never build view models in a parent's body (ContentView now owns TabModels);
     `waitForExistence` is not enough — RasoiUITests/Support/UITestHelpers.swift adds
     waitUntilHittable/waitAndTap/scrollUntilHittable, and `-uiTestingTab <name>` opens the app
     straight onto a tab instead of driving the tab bar.
TASK 056 done: QuickSnacks engine (+9 tests), TodayViewModel (+8 tests), TodayView (hero dinner, why-this, expiring banner, snack strip, coverage dots), FeedbackView pushed after cooking; TodayUITests green; ContentView now owns the tab view models.
TASK 057 done: InsightsViewModel (+10 tests: per-child favourites/not-lately in warm words, most cooked, variety, weekly hint), InsightsView, per-day coverage dots and the light-on hint on Plan.
TASK 058 implemented, awaiting human review (docs/COPY-NUTRITION.md — 10 strings + the MyPlate table and its source; regenerate with scripts/gen-copy-doc.py nutrition).
TASK 059 done: NotificationScheduler behind a protocol (pure notifications(for:) + snapshot), 12 tests via a fake centre (one of each, quiet window 21:00-07:00, switches, idempotent refresh), NotificationsSettingsView; notification copy added to AppCopy and the 043 review doc.
TASK 060 done: Exporter (household, diet, stores, pantry, user recipes, seeded-recipe flags, plans, feedback, lists; seeded catalog excluded) +6 tests; ExportView with ShareLink, AppearanceView; every More row is now a real screen.
TASK 061 done: AccessibilityUITests (5 tabs at accessibility XL, reachable primary actions, labelled icon buttons, spoken coverage dots), decorative icons hidden, docs/A11Y.md checklist.
TASK 062 done: scripts/make-app-icon.swift renders the 1024px saffron pot-and-leaf icon into the asset catalog; empty states on Plan, Pantry and Shop now carry the one action that fills them.
TASK 063 done: scripts/privacy-audit.sh checks sources, the store finder's imports, dependencies, entitlements, background modes and otool on the built binary; docs/PRIVACY-AUDIT.md written and clean.
TASK 064 done: docs/SPEC-AUDIT.md maps every red line, screen, engine rule and acceptance line to code and tests, with six explicit deviations; added the plan long-press context menu and a sub-second planning performance test (379 tests green).
TASK 065 done: scripts/screenshots.sh + ScreenshotUITests capture 22 screens; docs/REVIEW.md records each with OK or the fix; fixed cook-mode footer overlap, chip hyphenation, unit-blind default quantities, stale tab data on switch, and per-body-evaluation view model rebuilds.
TASK 066 implemented, awaiting human review (docs/REVIEW.md finalised; device install is human-gated — say the phone is cabled and give the team ID).
ALL 62 AUTO TASKS PASS. verify.sh green (385 test cases), privacy-audit.sh clean.
Open HUMAN gates: 028 (seed catalog), 043 (feedback/settings copy), 053 (MapKit privacy),
058 (nutrition copy), 066 (final review + iPhone install).
TASK 066 blocked on a human step: the iPhone 14 Pro (FCE9725F-A4EF-5A45-845D-BC015674C826) is
  paired and available, and a valid "Apple Development: shikher20goel@gmail.com" certificate exists
  (team OU PL36AJ998G), but xcodebuild -allowProvisioningUpdates fails with:
    "No Accounts: Add a new account in Accounts settings"
    "No profiles for 'com.shikhergoel.rasoi' were found"
  Xcode has no Apple ID signed in, so it cannot create a development provisioning profile.
  Human step: Xcode › Settings › Accounts › + › Apple ID (shikher20goel@gmail.com), then re-run
  the install block in docs/REVIEW.md. project.yml was restored so no team ID is committed.
TASK 066: Rasoi built signed (team PL36AJ998G, Apple Development: shikher20goel@gmail.com) and
  INSTALLED on Shikher Phone (iPhone 14 Pro, FCE9725F-A4EF-5A45-845D-BC015674C826); launched with
  devicectl. Free team: reinstall every 7 days. project.yml restored, no team id committed.
  Human gates 028/043/053/058 approved by Shikher; 066 install done.
FIX: the real team id had leaked into project.yml in commit f8367ca (a `git add -A` picked up the
  file while signing was configured). Restored to DEVELOPMENT_TEAM: "" and committed. History is
  not rewritten — the repo is private and a team id is not a credential — but the working tree and
  HEAD are clean. configure-signing.sh must always be followed by `git checkout project.yml`
  BEFORE any commit.
